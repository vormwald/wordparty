<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Word Party" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= hotwire_livereload_tags if Rails.env.development? %>
  </head>

  <body class="h-full bg-white dark:bg-stone-800 text-gray-900 dark:text-gray-100" data-controller="mobile-menu" data-action="click@window->mobile-menu#clickOutside">
    <header class="absolute inset-x-0 top-0 z-50 flex h-16 border-b border-gray-900/10 dark:border-gray-100/10">
      <div class="mx-auto flex w-full max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8">
        <div class="flex items-center">
          <%= link_to root_path, class: "flex items-center" do %>
            <span class="text-lg font-mono font-bold tracking-widest uppercase text-gray-900 dark:text-gray-100">Word Party</span>
          <% end %>
        </div>
        <nav class="hidden md:flex md:gap-x-11 md:text-sm md:font-semibold md:leading-6 md:text-gray-700 dark:md:text-gray-300">
          <%= link_to "Home", root_path %>
          <%= link_to "My Games", games_path %>
          <%= link_to "New Game", new_game_path %>
        </nav>
        <div class="flex items-center gap-x-8">
          <% if authenticated? %>
            <div class="relative hidden md:block" data-controller="dropdown">
              <button type="button" class="-m-1.5 flex items-center p-1.5" data-action="click->dropdown#toggle">
                <span class="sr-only">Open user menu</span>
                <img class="h-8 w-8 rounded-full bg-gray-50" src="https://images.unsplash.com/photo-1472099645785-5658abf4ff4e?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=facearea&facepad=2&w=256&h=256&q=80" alt="">
                <span class="hidden lg:flex lg:items-center">
                  <span class="ml-4 text-sm font-semibold leading-6 text-gray-900 dark:text-gray-100" aria-hidden="true"><%= current_user.email %></span>
                  <svg class="ml-2 h-5 w-5 text-gray-400" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 11.168l3.71-3.938a.75.75 0 111.08 1.04l-4.25 4.5a.75.75 0 01-1.08 0l-4.25-4.5a.75.75 0 01.02-1.06z" clip-rule="evenodd" />
                  </svg>
                </span>
              </button>
              <div class="absolute right-0 mt-2 w-48 bg-white dark:bg-stone-700 rounded-md shadow-lg py-1 z-10 hidden" data-dropdown-target="menu">
                <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "block w-full text-left px-4 py-2 text-sm text-gray-700 dark:text-gray-200 hover:bg-gray-100 dark:hover:bg-stone-600" %>
              </div>
            </div>
          <% else %>
            <%= link_to "Log in", new_session_path, class: "hidden md:block text-sm font-semibold leading-6 text-gray-900 dark:text-gray-100" %>
          <% end %>
          <button type="button" class="md:hidden" data-action="mobile-menu#toggleMenu">
            <span class="sr-only">Open main menu</span>
            <svg class="h-6 w-6 text-gray-900 dark:text-gray-100" viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
        </div>
      </div>
    </header>

    <!-- Mobile menu, show/hide based on menu state -->
    <div class="md:hidden" data-mobile-menu-target="menu">
      <div class="fixed inset-0 z-50"></div>
      <div class="fixed inset-y-0 right-0 z-50 w-full overflow-y-auto sm:border-l border-gray-900/10 dark:border-gray-100/10 bg-white dark:bg-stone-800 px-6 py-6 sm:max-w-sm sm:ring-1 sm:ring-gray-900/10">
        <div class="flex items-center justify-between">
          <a href="#" class="-m-1.5 p-1.5">
            <span class="sr-only">Word Party</span>
            <span class="text-lg font-mono font-bold tracking-widest uppercase text-gray-900 dark:text-gray-100">Word Party</span>
          </a>
          <button type="button" class="-m-2.5 rounded-md p-2.5 text-gray-700 dark:text-gray-300" data-action="mobile-menu#closeMenu">
            <span class="sr-only">Close menu</span>
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
        <div class="mt-6 flow-root">
          <div class="-my-6 divide-y divide-gray-500/10">
            <div class="space-y-2 py-6">
              <%= link_to "Home", root_path, class: "block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-stone-700" %>
              <%= link_to "My Games", games_path, class: "block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-stone-700" %>
              <%= link_to "New Game", new_game_path, class: "block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-stone-700" %>
            </div>
            <div class="py-6">
              <% if authenticated? %>
                <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "block rounded-lg px-3 py-2.5 text-base font-semibold leading-7 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-stone-700" %>
              <% else %>
                <%= link_to "Log in", new_session_path, class: "block rounded-lg px-3 py-2.5 text-base font-semibold leading-7 text-gray-900 dark:text-gray-100 hover:bg-gray-50 dark:hover:bg-stone-700" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <main class="py-24">
      <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <%= render 'shared/flash_messages' %>
        <%= yield %>
      </div>
    </main>
  </body>
</html>
